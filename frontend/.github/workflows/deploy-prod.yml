name: Deploy to Production
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Frontend
        run: |
          echo "Deploying frontend..."
          # Insert your deployment logic here (e.g., rsync, scp, or cloud CLI)
